Flask>=3.0,<4.0
Werkzeug>=3.0,<4.0
python-dotenv>=1.0,<2.0

# Desktop app (pywebview + PyInstaller). Used by desktop.py and scripts/build_desktop.py.
# pywebview wraps the native webview (WebKit on macOS, WebView2/Edge on Windows).
pywebview>=5.0,<6.0
platformdirs>=4.0,<5.0
waitress>=3.0,<4.0
pyinstaller>=6.0,<7.0
